program posdesign;

uses
  System.StartUpCopy,
  FMX.Forms,
  posdesignDelphi.view.index in 'view\posdesignDelphi.view.index.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
